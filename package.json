{
  "name": "SnowBall",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "electron-dev": "concurrently \"cd snowball_backend && npm start\" \"cd snowball_frontend && npm run dev\" \"wait-on http://localhost:5000 && wait-on http://localhost:3000 && cross-env NODE_ENV=development electron .\"",
    "build": "cd snowball_frontend && npm run build && cd .. && electron-builder",
    "pack": "electron-builder --dir",
    "dist": "electron-builder"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "wait-on": "^7.2.0"
  },
  "build": {
    "appId": "com.snowball.app",
    "productName": "SnowBall",
    "asar": false,
    "files": [
      "main.js",
      "preload.js",
      "snowball_frontend/out/**/*",
      "snowball_backend/**/*",
      "!snowball_backend/node_modules/**/*"
    ],
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": "nsis"
    }
  }
}